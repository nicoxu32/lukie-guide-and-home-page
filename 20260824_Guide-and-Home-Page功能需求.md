# Lukie App 引导流程功能需求

## 按钮状态

- 页面存在必填内容时，用户未完成全部必填项前，主操作按钮应为禁用状态，不可点击。
- 禁用状态需与可点击状态有明确的视觉区分。
- 当所有必填项填写完整且校验通过后，按钮自动切换为可点击状态。
- 禁用状态下点击按钮不触发页面跳转或数据提交。

### “Lukie 激活成功啦”页面

- 用户未填写任何一项宝贝信息时，“保存并继续”按钮为禁用状态，不可点击。
- 用户至少填写一项内容后，“保存并继续”按钮切换为可点击状态，允许保存当前已填内容。
- “先不填，和 Lukie 慢慢探索”不受上述规则影响，始终可点击。

## 首日功能引导蒙版

- 5 个引导蒙版均需展示 Lukie 的形象或功能图示。
- 形象和图示需与当前引导步骤的内容直接对应，帮助用户快速理解该步骤。
- 优先使用 GIF 动图展示 Lukie 的动作、设备状态或操作反馈。
- 例如：“Hi，Lukie”唤醒步骤需展示呼唤 Lukie 的场景；按键唤醒步骤需展示屏幕上出现“小耳朵”的图示或动效。
- GIF 需支持循环播放，在蒙版打开时自动播放，且不影响文字阅读和操作按钮。

## 首页初始态与新手引导

- 用户首次进入首页并展示蒙版新手引导时，首页应使用设计稿中的“有数据示例态”作为引导背景。
- 蒙版引导需基于该示例数据，依次介绍首页的主要模块和功能，避免因首页无真实数据而无法完整展示功能。
- 示例数据仅用于新手引导，不作为用户的真实业务数据保存。
- 用户完成或跳过全部新手引导后，关闭蒙版并退出示例态，返回首页日常态。
- 返回日常态时需根据用户的真实数据判断：有数据时展示有数据状态；无数据时展示对应的空状态。

## 首页内容与交互

### 头部 3D Lukie 形象

- 首页头部的 3D 蓝色 Lukie 大头形象，需根据心情分数所在的状态分段，展示对应的表情或状态图。
- 各分数段与具体形象的对应关系待补充。
- 责任人：@设计师。

### 今日话题标签

- 今日话题标签以 Emoji 展示。
- 用户点击某个 Emoji 后，在该 Emoji 的正上方展示气泡角标，气泡箭头需准确指向当前选中的 Emoji。
- 角标中展示该 Emoji 所代表的话题标签，文案统一采用 `#标签名` 格式，例如 `#创造力`、`#艺术表达`、`#坚持与成长`。
- 点击其他 Emoji 时，角标需移动至新选中的 Emoji 上方，并同步切换对应的标签内容。
- 再次点击当前已选中的 Emoji，关闭气泡角标并取消选中状态。

### 首页头部最近对话提示

- 首页头部可展示最近一条对话入口，示例：`18:57 🌙 为什么陆地上最大型的爬行动物是科莫多巨蜥？ ›`。
- 该功能仅在国内 iLuki 版本中提供。
- 除国内 iLuki 外，其他国内及海外版本保持一致，均不展示该入口。

### 今日总结配图

- “今日总结”右侧配图使用固定图库，预计约 XX 张。
- 配图根据“时间 + 场景”维度，从固定图库中匹配选择。
- 不再通过 AI 临时生成配图。
- 具体图片数量、时间分段、场景分类及匹配规则待整理。

### 成长明信片配图

- 成长明信片使用固定图库，预计约 60–70 张。
- 配图根据“时间 + 场景”维度，从固定图库中匹配选择。
- 不再通过 AI 临时生成明信片图片。
- 具体图片数量、时间分段、场景分类及匹配规则待整理。

### 成长明信片浏览

- “首页-日常态-有数据”和“首页-日常态-无数据”的今日成长明信片均采用横向滑动卡片轨道。
- 当首页已有其他数据，但当天没有生成成长明信片时，“今日成长明信片”模块仍需展示，并复用“首页-日常态-无数据”中的默认明信片图片及样式作为兜底内容。
- 首页默认加载并展示 3 张成长明信片。
- 当前卡片占据主要可视宽度，右侧需露出下一张卡片的部分内容，提示用户可以继续滑动。
- 用户可向左滑动，依次浏览今天的 3 张明信片。
- 第 3 张明信片之后展示“查看更多”引导卡片。
- 用户继续向左滑动至轨道末尾时，自动进入“成长高光明信片”图库页。
- 用户点击模块标题右侧的“查看更多 ›”或轨道末尾的“查看更多”卡片，也可直接进入图库页。
- “成长高光明信片”图库页汇总展示全部历史成长明信片，不限制张数。
- 图库页视觉参考 iPhone 相册的图片列表页。
- 相册列表采用每行 3 张图的网格布局。
- 每张图片上展示对应的日期和时间。
- 点击图片后，进入大图查看页。

### 时光流会话标签

- 时光流中每个会话需进行标签统计与展示。
- 标签建议优先使用“情绪标签”，而不是重复会话主题或事件内容。
- 原因：当标题、总结内容、标签和左侧图标都描述同一件事时，会造成信息重复，降低浏览效率。
- 建议的信息分工：标题说明发生了什么；总结补充关键细节；左侧图标表示场景或事件类型；标签表示宝贝当时的情绪状态。
- 情绪标签的分类、命名、颜色和统计口径待整理。
