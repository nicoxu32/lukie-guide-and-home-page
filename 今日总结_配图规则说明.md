# 今日总结配图规则说明

本文约定「今日总结」首页配图的选图规则。今日总结配图与成长高光明信片**不共用选图链路、不共用图库**（明信片见《成长高光明信片_配图规则说明-202608.md》）。

## 0. 两个方案与本版选择

今日总结配图有两个方案，**本版采用方案一（固定图 / 轮换）**，方案二作为后续演进。两套日级提示词已分别就绪：

| 维度 | 方案一：固定图 / 轮换（本版采用） | 方案二：场景 + 时段选图（后续） |
|---|---|---|
| 对应日级提示词 | `日级-洞察提示词_固定图提示词_202608.md`（不输出 `image_retrieval`） | `今日总结-洞察提示词-配图版本提示词.md`（输出 `image_scene` + `event_period`） |
| LLM 配图字段 | 无 | `image_scene`、`event_period`（不含 `image_keywords`） |
| 后端选图 | 不做场景 / 时段 / 情绪 / 关键词匹配；固定图或按主情绪 / 日期轮换直出 | 场景粗召回 + 时段过滤 + 情绪 / 风险安全过滤 + 近 7 次去重，**无关键词精排** |
| 独立图库 | 少量固定素材即可 | `daily_summary` 独立图库，需场景 × 时段深度 |
| 素材制作成本 | 低，制作周期短 | 中，需按 12 类场景 × 时段建库 |
| 幂等键 | `user_id + calendar_date` | `user_id + calendar_date` |
| 无候选处理 | 不涉及（固定图恒有） | 降级通用安全图；仍无则默认占位，不阻断今日总结 |

**建议**：本版先走方案一，把成长高光明信片的配图规则先完整跑通、验证选图链路与素材入库流程；待今日总结专属图库素材齐备后，再切换到方案二。切换时只需替换日级提示词与后端选图开关，不影响 `daily_title` / `daily_summary` 等文字生产。

---

# 方案一：固定图 / 轮换（本版采用）

## 一·1. 链路

```text
当天有效会话
→ 日级提示词（固定图版）生成 daily_title / daily_summary 等文字字段（不输出配图字段）
→ 后端按固定规则直出 1 张 daily_summary 图
→ 结果落库，首页稳定复用
```

## 一·2. 选图规则

- 不做场景 / 时段 / 情绪 / 关键词匹配，也不调用 AI 文生图。
- 采用一张固定图，或从一组少量固定素材中按稳定规则轮换直出。轮换维度择一，联调阶段定：
  - 按今日主情绪 `emotion_level_1`（9 类各 1 张）映射；
  - 按 `calendar_date` 轮换；
  - 单张全局固定图。
- 后端只需少量 `asset_type=daily_summary`、`is_default=false` 的固定素材。

## 一·3. 触发与幂等

- 仅当存在有效日级总结时出图；完全无有效会话 / 无有效总结时不出图，页面展示无数据状态。
- `daily_mood_score = null` 但有真实 `daily_title` / `daily_summary` 时仍出图。
- 幂等键 `daily_summary:{user_id}:{calendar_date}`；刷新和重试不换图。

## 一·4. 展示与异常

- 首页只读已落库结果，客户端不重选、不随刷新换图。
- 图片加载失败时展示客户端内置占位图，不改写结果、不新增记录、不创建新任务。

## 一·5. 采用理由

本版重点先跑通成长高光明信片配图链路；今日总结专属素材尚未制作，固定图素材制作周期短、可先上线，避免素材制作周期阻塞首页改版节奏。

---

# 方案二：场景 + 时段选图（后续，图库齐备后启用）

> 待今日总结专属 `daily_summary` 图库素材齐备、达到场景 × 时段可用深度后启用；日级提示词切换为 `今日总结-洞察提示词-配图版本提示词.md`（额外输出 `image_retrieval`）。以下为方案二的完整选图细则。

## 1. 结论

今日总结配图采用“日级总结先定调，后端固定图库选图”的链路：

```text
当天有效会话
→ LLM 生成 daily_title / daily_summary
→ LLM 基于日级主事件输出 image_scene / event_period
→ 后端复用 emotion_level_1 / risk_judgement 做安全过滤
→ 按场景、时段和近期去重从 daily_summary 图库选 1 张
→ 结果落库，首页稳定复用
```

今日总结只需要两个新增配图字段：`image_scene`、`event_period`。不复制成长高光明信片的 `image_keywords`，也不做主体 / 事件 / 动作精排。

这样做的原因是：高光明信片要尽量还原一个具体瞬间，关键词精排有明确收益；今日总结覆盖全天多个会话，本身只表达“今天的主主题和氛围”，继续做细粒度精排容易让图片看起来只对应某一段对话，且会增加 LLM 字段不稳定和图库标签维护成本。

## 2. 与成长高光明信片的关系

| 规则维度 | 成长高光明信片 | 今日总结 |
|---|---|---|
| 业务对象 | 单个会话中的高光时刻 | 一天内所有有效会话的整体总结 |
| 素材类型 | `growth_postcard` | `daily_summary` |
| LLM 新增字段 | `image_scene`、`event_period`、`emotion_level_1`、`image_keywords` | `image_scene`、`event_period` |
| 场景用途 | 粗召回具体场景 | 粗召回今日主场景 |
| 时段用途 | 过滤事件发生的白天 / 夜晚 | 过滤主事件时段；多时段使用 `any` |
| 精排 | 主体 +3、事件 +2、动作 +1 | 不精排 |
| 情绪用途 | 排除明显冲突氛围 | 排除明显冲突氛围；风险日进一步只保留安抚池 |
| 去重窗口 | 该用户最近 7 次 `growth_postcard` 图片 | 最近 7 次 `daily_summary` 图片 |
| 业务幂等键 | `session_id` | `user_id + calendar_date` |
| 无候选处理 | `no_match`，不生成明信片 | 使用通用安全图；仍无候选时展示默认占位，不阻断今日总结 |

共享的只有 12 类场景、时段判断、情绪排斥、风险安全、固定图库和稳定落库；两类业务不共享素材池，也不共享精排和去重窗口。

## 3. LLM 输出的配图召回包

日级提示词与今日总结字段一次调用输出：

```json
{
  "image_retrieval": {
    "image_scene": "朋友社交",
    "event_period": "day"
  }
}
```

### 3.1 `image_scene`

只能从以下 12 类中选择一个：

学习探索、绘画手工、音乐舞蹈、阅读故事、游戏娱乐、户外运动、动物自然、家庭陪伴、朋友社交、日常生活、情绪陪伴、通用。

规则：

- 依据 `daily_title` 和 `daily_summary` 中的主事件生成，再回看输入会话确认事实。
- 只选一个最能代表全天总结的主场景，不按 `daily_topic_tags` 的 TOP 1 直接决定。
- 多个场景并列、只有情绪/风险描述、或无法判断时，输出 `通用`。
- 篮球、跑步、踢足球等运动归入 `户外运动`。
- 不输出自定义场景、数组、Topic 标签、高光维度或情绪名称。

### 3.2 `event_period`

取值只有 `day`、`night`、`any`：

| 取值 | 使用条件 |
|---|---|
| `day` | 主事件明确发生在早晨、上午、中午、下午、白天或放学后天未黑 |
| `night` | 主事件明确发生在傍晚、晚上、夜里、睡前或昨晚 |
| `any` | 总结包含多个时段，或没有可靠的事件时间证据 |

时间证据优先级：会话正文明确说法 > 上下文继承 > 明确“正在发生”时才参考消息时间 > `any`。例如上午 11:00 提到“昨晚踢球”，输出 `night`，不能按消息时间输出 `day`。

## 4. 后端选图规则

### 4.1 触发和幂等

- 仅当存在有效日级总结时创建 `daily_summary` 选图任务。
- `daily_mood_score = null` 但有真实的 `daily_title` / `daily_summary` 时，仍可以选图。
- 完全无有效会话或无有效总结时，不创建选图任务，页面展示无数据状态。
- 任务幂等键为 `daily_summary:{user_id}:{calendar_date}`。
- 已有成功或处理中任务时复用原结果；页面刷新和重试不得重新换图。

### 4.2 候选硬过滤

候选必须同时满足：

1. `active = true`，资源文件可用，`asset_type = daily_summary`。
2. `is_default = false`；默认占位图不进入后端候选池。
3. 场景、时段和图片安全标签合法。
4. 通过情绪氛围过滤和风险过滤。

图库使用与高光明信片相同的 12 类 `scene_tags`、`period` 和 6 类安全氛围：`温暖`、`鼓励`、`平静`、`中性`、`欢快`、`庆祝`。每张图只标一个 `atmosphere`。

情绪排斥规则：

| 日级 `emotion_level_1` | 排除的 `atmosphere` |
|---|---|
| 开心、好奇 | 无 |
| 平静 | 庆祝 |
| 悲伤、沮丧、愤怒、焦虑、恐惧、压抑 | 欢快、庆祝 |

风险规则：

- 无风险命中：按上述情绪排斥规则选图。
- `risk_judgement.is_hit = true`：只允许 `温暖`、`平静`、`中性` 图片；不得使用欢快或庆祝图，也不得因为场景命中而放开风险过滤。
- 风险字段缺失或非法：按风险日处理，只查通用的温暖 / 平静 / 中性图库，并记录 `risk_field_missing`。

### 4.3 匹配和降级顺序

通过硬过滤后，按以下层级逐层查询；找到候选后停止，不跨层混排：

| 层级 | 场景 | 时段 |
|---|---|---|
| `scene_period` | `image_scene` 对应场景 | `event_period = day/night` 时匹配对应时段 |
| `scene_any_period` | `image_scene` 对应场景 | `event_period = any`，或精确时段无候选时，使用该场景任一安全时段图 |
| `generic_period` | `通用` | 输入为 `day/night` 时匹配对应时段 |
| `generic_any_period` | `通用` | 输入为 `any` 或前面没有候选时使用任一安全通用图 |

补充规则：

- `image_scene = 通用` 时直接从通用图库开始。
- 具体场景没有候选时，依次降级到同场景任一时段、通用同一时段、通用任一时段。
- `event_period = any` 时不得为了“更匹配”猜测 `day` 或 `night`。
- 所有降级仍必须通过情绪和风险硬过滤。

### 4.4 去重和最终选择

- 去重窗口为当前用户最近 7 次成功的 `daily_summary` 图片。
- 先从当前匹配层排除窗口内已使用的图片。
- 排除后仍有候选时，按 `last_used_at` 最早优先，再按 `image_id` 升序稳定选择。
- 当前层全部候选都重复时，恢复其中最久未使用的一张，并记录 `dedupe_exhausted`；不能为了去重跨过风险过滤。
- 选图、结果写入和使用记录写入需要在同一幂等控制下完成，避免并发生成同一天的不同图片。

## 5. 结果记录和前端展示

后端至少记录：

| 字段 | 说明 |
|---|---|
| `user_id`、`calendar_date` | 业务归属和幂等键 |
| `image_id`、`asset_type` | 最终素材及类型 |
| `image_scene`、`event_period` | LLM 原始配图字段 |
| `match_level` | 命中的匹配层级 |
| `fallback_reason` | `scene_fallback`、`period_fallback`、`dedupe_exhausted` 等原因 |
| `rule_version`、`created_at`、`updated_at` | 规则版本和任务时间 |

首页只读取已落库的成功结果，不在客户端重新判断场景、随机换图或根据刷新重新选图。图片资源加载失败时，临时展示客户端内置的今日总结占位图，不改写选图结果、不新增使用记录、不创建新任务。选图服务失败或超时时，优先返回已落库旧结果；没有旧结果时使用默认占位，不阻断标题、总结和其他首页模块。

## 6. 为什么这样简化

### 保留的能力

- 图片与今日总结的主场景一致。
- 白天 / 夜晚明显事件不会被完全忽略。
- 负向情绪和风险日不会匹配到庆祝型图片。
- 固定图库、去重、幂等和降级链路完整可实现、可追溯。

### 暂不做的能力

- 不输出 `image_keywords`。
- 不做主体 / 事件 / 动作三类标签精排。
- 不用 `emotion_level_1` 对图片做加权排序，只做明显冲突排除。
- 不使用 `daily_topic_tags`、高光 `dimension` 或 `healing_quote` 参与选图。
- 不为每个细粒度事件单独生产图片，也不调用 AI 文生图。

### 主要取舍

简化后，图片可能只能表达全天的主场景，无法精准复现当天某个具体事件；但它减少了 LLM 输出字段、图库标签数量和后端排序逻辑，能先验证“今日总结配图是否与整体内容一致”。如果上线抽检显示通用图或场景错配率较高，再按数据最多的场景补充关键词精排，而不是一开始为所有场景建设完整精排体系。

## 7. 最小验收样例

| 输入情况 | LLM 配图字段 | 后端行为 |
|---|---|---|
| 全天主要是搭乐高，白天完成 | `游戏娱乐` / `day` | 先选游戏娱乐白天安全图 |
| 上午提到昨晚踢球，今日总结主要写这件事 | `户外运动` / `night` | 选户外运动夜间图，不能按消息时间选白天图 |
| 同时有学习、家庭陪伴和校园冲突，无法取一个主场景 | `通用` / `any` | 从通用安全图中选择 |
| 日级风险命中，主场景为朋友社交 | `朋友社交` / `day` | 只从朋友社交或降级通用的温暖 / 平静 / 中性图中选择 |
| 无有效会话 | 不生成 | 页面展示无数据状态，不创建选图任务 |

---

## 8. 版本记录

### 2026-09-01

- 新增今日总结独立配图规则，参考成长高光明信片的场景、时段、情绪安全、风险和去重框架。
- 日级配图简化为 `image_scene + event_period` 两个 LLM 字段，不引入 `image_keywords` 精排。
- 明确今日总结使用独立 `daily_summary` 图库、最近 7 次去重和 `user_id + calendar_date` 幂等键。
- 重构为「方案一：固定图 / 轮换（本版采用）」与「方案二：场景 + 时段选图（后续）」两方案结构：方案一对应 `日级-洞察提示词_固定图提示词_202608.md`（不输出配图字段），方案二对应 `今日总结-洞察提示词-配图版本提示词.md`（输出 `image_scene` + `event_period`）；本文原有选图细则整体归入方案二。
