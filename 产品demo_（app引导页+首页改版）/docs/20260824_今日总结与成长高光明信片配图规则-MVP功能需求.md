---
requirement_id: req-home-insight-image-mvp
status: draft
targets:
  - server
  - mobile
modules:
  - daily-insight
  - growth-postcard
  - home
acceptance:
  functional: required
  visual: excluded
---

# 今日总结与成长高光明信片配图规则 MVP 功能需求

## 1. 结论与范围

本需求将“成长高光明信片”和“今日总结”从 AI 内容分析到首页展示的配图链路统一为一个最小闭环：

```text
对应业务范围的 AI 总结
→ LLM 输出固定 image_scene
→ LLM 输出 event_period
→ 复用已有 emotion_level_1 排除氛围冲突
→ 复用已有 risk_judgement 执行风险规则
→ 后端按 asset_type 召回、去重、兜底并选图
→ 结果落库后由首页稳定复用
```

其中：

- 成长高光明信片针对单个会话，依据当前会话的 `session_summary + highlight_info.dialogue_snippet` 生成配图字段。
- 今日总结针对当天全部会话，先生成 `daily_title + daily_summary`，再依据全天总结生成配图字段。
- 两个功能共享场景枚举、时段规则、图片元数据、选图服务、使用记录和兜底框架。
- 两个功能不共享 AI 总结范围、风险处理、去重窗口和业务幂等键。
- `topic_tags` 继续服务原有话题展示与统计，不参与任何配图。
- 高光 `dimension_id/dimension` 继续服务高光解释与统计，但不参与 MVP 配图。
- LLM 不输出图片 ID；最终图片由后端从固定图库中确定性选择。

本方案的关键取舍是：只新增两个业务语义字段，减少 Topic 聚合、高光主题、成长等级、情绪弧线等中间层。这样对旧提示词和数据契约的改动最小，但 MVP 只能做到 12 类粗场景匹配，无法覆盖所有细粒度事件。

### 1.1 端到端共享底座：7 个阶段

7 个阶段是两项功能共享的处理骨架，但每个阶段的业务范围不同。下表分别列出输入、输出和当前逻辑，避免把“共享底座”误解为“使用同一份总结或同一张图”。

| 阶段 | 共享方式 | 成长高光明信片：输入 → 输出 | 今日总结：输入 → 输出 | 当前逻辑 |
| --- | --- | --- | --- | --- |
| 1. 会话分析 | 共用会话级 `session_insight` 入口 | 输入：`raw_session`、`session_id`、`calendar_date`、`profile.timezone`、`output_language_name`。输出：原有会话心情、标题、总结、情绪、Topic、风险、高光评分/维度/原文 | 输入相同。输出的全部 `session_insight` 作为日级聚合原料 | 已有会话级提示词和落库；高光、风险、情绪、Topic 已存在 |
| 2. 对应范围总结与配图字段生成 | 共用“先总结、后归一场景/时段”的方法和枚举 | 输入：当前 `session_summary`、高光 `dialogue_snippet`、会话原文时间证据。输出：`highlight_info.image_scene`、`highlight_info.event_period` | 输入：当天全部有效 `session_insight`。先输出 `daily_title/daily_summary`，再输出日级 `image_scene/event_period` | 当前高光已有总结和原文但无固定图库字段；日级已有标题/总结但无配图字段 |
| 3. 资格与任务触发 | 共用任务状态、重试框架；条件不同 | 输入：`has_highlight`、`highlight_score`、会话 `risk_judgement`、`session_id`。输出：是否创建 `growth_postcard` 选图任务；幂等键 `session_id` | 输入：有效 `daily_insight`、`user_id`、`calendar_date`。输出：是否创建 `daily_summary` 选图任务；幂等键 `user_id + calendar_date` | 当前只有高光分数判断和日级洞察任务，没有固定图库选图任务与完整状态 |
| 4. 图库候选召回 | 共用图片目录和查询服务，按 `asset_type` 隔离候选池 | 输入：`asset_type=growth_postcard`、`image_scene`、`event_period`、启用状态。输出：高光明信片候选图片列表及元数据 | 输入：`asset_type=daily_summary`、`image_scene`、`event_period`、启用状态。输出：今日总结候选图片列表及元数据 | Demo 为静态素材；原高光使用 AI 生图，尚无统一固定图库召回 |
| 5. 情绪/风险过滤、去重与兜底 | 共用筛选引擎；安全规则和窗口不同 | 输入：候选、会话 `emotion_level_1/risk_judgement`、近 30 天及同日使用记录。输出：最终 `image_id`、`match_level`、`fallback_reason`；风险命中时无明信片 | 输入：候选、日级 `emotion_level_1/risk_judgement`、最近 7 次使用记录。输出：最终 `image_id`、`match_level`、`fallback_reason`；风险日只保留中性/安抚候选 | 当前没有统一后端过滤、去重和兜底规则 |
| 6. 结果落库与可解释记录 | 共用结果表/等价模型，业务键不同 | 输入：任务键、匹配字段、最终图片、图库版本。输出：按 `session_id` 唯一的选图结果与使用记录 | 输入：任务键、匹配字段、最终图片、图库版本。输出：按 `user_id + calendar_date` 唯一的选图结果与使用记录 | 当前未统一记录 `asset_type/match_level/fallback_reason/rule_version` |
| 7. 首页消费与降级展示 | 共用稳定读取、资源失败和占位机制 | 输入：会话绑定的高光与图片结果。输出：明信片卡片/详情；不合格时不伪造高光卡，资源失败显示高光默认图 | 输入：业务日绑定的日级洞察与图片结果。输出：首页今日总结图片；无有效对话或服务失败显示无数据/默认占位图 | Demo 已有静态展示态，尚未接入真实选图结果和失败状态 |

阶段 1 是共同数据入口；阶段 2 使用同一方法但总结范围不同；阶段 3 开始按业务触发条件分叉；阶段 4–7 再共享同一套固定图库基础设施。

### 1.2 两条完整业务链路

成长高光明信片：

```text
会话结束
→ 生成完整 session_insight
→ 根据 session_summary + 高光原文输出 highlight_info.image_scene/event_period
→ 后端校验 has_highlight=true、highlight_score>=7、risk_judgement.is_hit=false
→ 以 session_id 幂等创建 growth_postcard 选图任务
→ 按“场景 → 时段 → 情绪 → 去重 → 兜底”选图
→ 保存 image_id/match_level/fallback_reason
→ 首页明信片卡片和成长图库读取固定结果
```

今日总结：

```text
会话结束并按档案时区归入 calendar_date
→ 日级 LLM 读取当天全部 session_insight
→ 先生成 daily_title + daily_summary
→ 再输出日级 image_scene/event_period
→ 以 user_id + calendar_date 幂等创建 daily_summary 选图任务
→ 按“场景 → 时段 → 情绪/风险 → 去重 → 兜底”选图
→ 保存 image_id/match_level/fallback_reason
→ 首页今日总结读取固定结果
```

### 1.3 范围边界

MVP 包含：

- 新增版会话级、日级提示词及完整输入输出契约。
- 会话高光与日级洞察各新增 `image_scene/event_period`。
- 12 类共享场景枚举和 `day/night/any` 时段规则。
- 两类图片元数据、候选召回、情绪/风险过滤、软去重、兜底和幂等记录。
- 首页无数据、未达高光、图库失败和资源失败的降级展示。
- 埋点和上线后质量指标。

MVP 不包含：

- 不修改或删除旧提示词文件。
- 不由 LLM 输出图片 ID或直接访问图库。
- 不用 Topic Top 1、话题频次或 `co_emotion` 决定图片。
- 不用高光维度、视觉主题、成长等级或情绪弧线决定图片。
- 不继续使用高成本 AI 文生图生成明信片。
- 不承诺覆盖所有细分事件，也不承诺在候选不足时完全不重复。
- 不包含图片生产与画风 SOP、用户手动换图和历史全量回算。

---

## 2. 背景与问题

### 2.1 今日总结

现有首页今日总结需要固定图片，但配图链路没有稳定的日级场景字段、候选筛选顺序、兜底和可解释记录。如果从单个会话 Topic Top 1 推图，会出现“今天所有会话的总结内容”和“某一个高频话题图片”不一致的问题。

### 2.2 成长高光明信片

原方案在单个会话达到高光后，把会话总结等内容交给 AI 文生图。实际问题是生成成本高、耗时不可控、画风与氛围不稳定，高危或负向内容还可能被错误包装成庆祝画面。

### 2.3 共同问题

此前方案为了提高匹配精度，增加了 Topic 场景、Topic 时段、共现情绪、高光主题、成长等级、情绪弧线等字段，导致同一事实被多次抽取和传递。MVP 需要先验证一个更简单的问题：AI 总结完成后，能否稳定输出一个粗场景和时段，并由后端用已有情绪、风险字段完成安全选图。

---

## 3. 两个功能的关系与差异

### 3.1 共享底层调整

| 共享调整 | 解决哪个功能 | 基本逻辑 | 为什么需要 |
| --- | --- | --- | --- |
| `image_scene` 12 类枚举 | 两个功能 | 将对应范围的总结归一为有限图库场景 | 让 LLM 输出可被后端稳定查询 |
| `event_period` 三值枚举 | 两个功能 | 事件时间优先，未知时 `any` | 避免白天说“昨晚”的昼夜错配 |
| `emotion_level_1` 氛围过滤 | 两个功能 | 复用已有一级情绪排除明显冲突图片 | 不新增重复的 `visual_tone` 或 Topic 情绪字段 |
| `risk_judgement` 安全规则 | 两个功能 | 高光风险会话拦截；今日总结风险日只用中性/安抚图 | 防止风险事件被庆祝化 |
| `asset_type` 图片池隔离 | 两个功能 | 区分 `growth_postcard` 与 `daily_summary` | 两类卡片画幅/设计可不同，不能仅凭文件名判断 |
| 图片使用记录 | 两个功能 | 记录最近使用、匹配等级、兜底原因和图库版本 | 支持去重、问题追踪和效果统计 |

### 3.2 不共享的业务规则

| 规则 | 成长高光明信片 | 今日总结 |
| --- | --- | --- |
| AI 分析范围 | 单个会话总结 + 高光原文 | 当天全部会话的日级标题 + 总结 |
| 生成单位 | 每个合格会话最多 1 张 | 每个用户每个业务日 1 张 |
| 资格 | 高光分数达标且未命中风险 | 当天存在有效日级总结 |
| 情绪字段 | 会话 `emotion_level_1` | 日级 `emotion_level_1` |
| 风险处理 | `risk_judgement.is_hit=true` 时不生成 | 仍可生成，但只能用中性/安抚图 |
| 去重窗口 | 近 30 天 + 同日两类图片 | 最近 7 次实际使用 |
| 幂等键 | `session_id` | `user_id + calendar_date` |

---

## 4. 术语与统一枚举

| 术语 | 定义 |
| --- | --- |
| `calendar_date` | 工程侧依据 `profile.timezone` 计算出的业务自然日 |
| `message_time` | 消息发送或会话发生的系统时间，只能在明确“正在发生”时辅助判断事件时段 |
| `event_period` | 总结中代表事件的发生时段：`day`、`night`、`any` |
| `image_scene` | 面向固定图库的 12 类中文场景枚举 |
| `asset_type` | 图片业务类型：`growth_postcard` 或 `daily_summary` |
| `match_level` | 最终图片的匹配/降级层级 |
| `fallback_reason` | 未能精确匹配或去重失败的可解释原因 |
| 通用图 | `primary_scene=通用` 或 `period=any` 的兜底图片 |

### 4.1 `image_scene`

固定为：

1. 学习探索
2. 绘画手工
3. 音乐舞蹈
4. 阅读故事
5. 游戏娱乐
6. 户外运动
7. 动物自然
8. 家庭陪伴
9. 朋友社交
10. 日常生活
11. 情绪陪伴
12. 通用

映射原则：具体事实场景优先；存在多个场景时选择当前总结中最具代表性的场景；无主场景、多个场景并列或证据不足时使用 `通用`。

### 4.2 `event_period`

| 值 | 判断规则 |
| --- | --- |
| `day` | 明确为早晨、上午、中午、下午、白天、放学后且天未黑等 |
| `night` | 明确为傍晚、晚上、夜里、睡前、昨晚等 |
| `any` | 没有可靠时间证据，或代表内容跨昼夜/时段冲突 |

判定优先级固定为：

```text
明确事件时间
> 上下文继承的事件时间
> 明确正在发生时参考本地 message_time
> 无法判断时 any
```

禁止仅根据消息发送时间推断事件时间。例如上午 11:00 说“昨晚和爸爸踢球”，必须为 `night`。

---

## 5. 字段调整总表

### 5.1 会话级 LLM 输入

| 字段 | 来源标记 | 说明 | 配图用途 |
| --- | --- | --- | --- |
| `raw_session` | 原有 | 孩子与 lukie 的逐轮原文/ASR、角色和时间 | 高光事实与事件时间来源 |
| `session_id` | 原有 | 会话唯一标识 | 明信片幂等绑定，不由 LLM 输出 |
| `calendar_date` | 原有 | 已按档案时区确定的业务日 | 结果归档；不是时区规则 |
| `profile.timezone` | 原逻辑隐含，本版显式 | 将系统时间换算为孩子当地时间的规则 | 仅辅助明确实时事件的时段判断 |
| `output_language_name` | 原有 | 展示字段输出语言 | 不参与选图 |

`profile.timezone` 是“怎么算当地日期和时间”，`calendar_date` 是“最终算出来是哪一天”。工程应优先传已换算的本地消息时间，LLM 不自行推导业务日。

### 5.2 会话级 LLM 输出

| 字段 | 来源标记 | 规则 | 使用方 |
| --- | --- | --- | --- |
| `session_mood_score`、`mood_state` | 原有 | 保持旧版心情逻辑和九档映射；无数据分值为 `null` | 会话展示、日级聚合 |
| `session_title`、`session_summary` | 原有 | 保持旧版事实标题和家长汇报总结逻辑 | 展示、日级输入、高光配图语义 |
| `emotion_level_1` | 原有 | 九类中文主情绪 | 两类图片氛围过滤 |
| `emotion_labels_level_2` | 原有 | 二级情绪标签与频次 | 统计，不直接配图 |
| `topic_tags` | 原有 | 1–6 个话题标签、类型、风险分类、频次、共现情绪 | 展示/统计，不参与配图 |
| `risk_judgement`、`risk_ai_guidance` | 原有 | 会话风险与家长建议 | 高光资格拦截、风险展示 |
| `highlight_info.highlight_score` | 原有 | 0–10 分 | 高光资格 |
| `highlight_info.has_highlight` | 原有 | 分数 `>=7` 为 `true` | 高光资格 |
| `highlight_info.dimension_id/dimension` | 原有 | 6 类高光维度 | 高光解释/统计，不参与 MVP 配图 |
| `highlight_info.dialogue_snippet` | 原有 | 1 条孩子原话；无有效对话为空数组 | 高光展示、配图事实核对 |
| `highlight_info.image_scene` | 新增 | 根据当前 `session_summary + dialogue_snippet` 输出 12 类之一 | 高光明信片场景召回 |
| `highlight_info.event_period` | 新增 | 高光代表事件时段：`day/night/any` | 高光明信片昼夜过滤 |
| `healing_quote` | 原有 | 会话疗愈金句 | 展示，不参与配图 |

不新增：`postcard_eligible`、`highlight_topic`、`highlight_theme`、`growth_level`、`emotional_arc`、`visual_tone`。

### 5.3 `topic_tags[]` 保持原有边界

```json
{
  "label": "#⚽Football",
  "standard_tag": "足球",
  "type": "normal",
  "risk_category": "",
  "count": 3,
  "co_emotion": "开心"
}
```

- 以上字段由会话级 LLM 输出。
- SQL 只聚合 `standard_tag/count` 等现有统计，不新增话题子字段。
- `co_emotion` 保留历史洞察用途，不参与两类配图。
- `topic_tags[]` 不增加 `image_scene/event_period/scene_hint`。
- 今日话题 Top 1 与今日总结图片是两个独立结果，不能互相覆盖。

### 5.4 日级 LLM 输入

| 字段 | 来源标记 | 说明 | 配图用途 |
| --- | --- | --- | --- |
| `daily_sessions` | 原有 | 当天全部有效 `session_insight` | 全天总结的唯一事实输入 |
| `calendar_date` | 原有 | 业务自然日 | 今日总结幂等绑定 |
| `output_language_name` | 原有 | 展示字段输出语言 | 不参与选图 |

不新增 `daily_top_topics` 配图输入。SQL 可继续为话题榜单生成 Top N，但日级提示词不依赖该结果。

### 5.5 日级 LLM 输出

| 字段 | 来源标记 | 规则 | 使用方 |
| --- | --- | --- | --- |
| `daily_mood_score`、`daily_mood_state` | 原有 | 保持旧版全天加权和九档状态 | 今日总结展示 |
| `daily_title`、`daily_summary` | 原有 | 综合当天全部有效会话，先于配图字段生成 | 展示、配图语义输入 |
| `emotion_level_1` | 原有 | 九类中文全天主导情绪 | 今日总结图片氛围过滤 |
| `risk_judgement`、`risk_ai_guidance` | 原有 | 全天 OR、最高风险等级、类别合并 | 风险展示与图片安全过滤 |
| `healing_quote` | 原有 | 日级重新生成的疗愈金句 | 展示，不参与配图 |
| `image_scene` | 新增 | 根据 `daily_title + daily_summary` 输出 12 类之一 | 今日总结图片场景召回 |
| `event_period` | 新增 | 总结代表事件时段：`day/night/any` | 今日总结图片昼夜过滤 |

日级 LLM 仍不输出 `emotion_labels_level_2`、`daily_topic_tags`、`daily_top_topics` 或图片 ID。

---

## 6. 图片库与元数据

### 6.1 图片元数据契约

```json
{
  "id": "postcard_friends_day_03",
  "file": "postcard_friends_day_03.png",
  "asset_type": "growth_postcard",
  "primary_scene": "朋友社交",
  "period": "day",
  "mood": ["warm", "cheerful"],
  "is_generic": false,
  "active": true,
  "asset_version": "2026-08-mvp"
}
```

| 元数据 | 规则 |
| --- | --- |
| `id` | 图片稳定唯一 ID，不使用文件名作为业务主键 |
| `file` | 资源文件名或可解析资源地址 |
| `asset_type` | `growth_postcard` 或 `daily_summary`；跨业务复用时建立明确多值关系，不靠猜测 |
| `primary_scene` | 12 类 `image_scene` 之一 |
| `period` | `day`、`night`、`any` |
| `mood` | 至少一个：`cheerful`、`warm`、`calm`、`encouraging`、`neutral`、`celebratory` |
| `is_generic` | 是否为通用兜底图 |
| `active` | `false` 时不得进入新候选，但保留历史记录 |
| `asset_version` | 图库版本，支持回溯与灰度 |

本版不需要图片维护 `highlight_dimension`、`highlight_theme` 或 `growth_level`。

### 6.2 图库数量逻辑

图片数量由“场景 × 时段 × 每个候选池的最低去重深度”决定，不再由高光维度或 Topic 数量决定。

| 版本 | 成长高光明信片 | 今日总结 | 目标与取舍 |
| --- | --- | --- | --- |
| MVP | 约 72–96 张：11 个具体场景 × 2 时段 × 每格 3–4 张，再配 6–8 张通用/`any` 图 | 约 120 张：11 个具体场景 × 2 时段 × 每格 4 张，再配约 32 张通用、`any` 和重点氛围图 | 验证匹配准确性；去重为软规则，细粒度事件会落通用图 |
| 标准版 | 约 120–180 张；高频场景每格 6–8 张，中频 4–5 张，低频至少 3 张 | 约 180–240 张；按真实场景频率和正/负氛围扩容 | 提升高频场景去重和情绪适配，不追求平均铺图 |
| 完整版 | 约 240–360 张，按“近 30 天重复率”和具体场景错配率动态扩容 | 约 300–480 张，按“最近 7 次重复率”和通用图兜底率动态扩容 | 以线上覆盖率为目标，不把张数当唯一完成标准 |

完整版扩容公式：先按实际 `asset_type + image_scene + event_period + emotion_bucket` 分布计算每个候选池需求；当某池的重复率、兜底率或错配率超过阈值时，只扩该池，不平均增加所有图片。

MVP 两类图库可在视觉允许时复用底图，但必须分别标记 `asset_type` 并满足各自画幅与版式；总张数不能直接用两栏相加判断制作量，应以最终可复用素材清单为准。

---

## 7. 情绪与风险过滤

### 7.1 一级情绪到图片氛围

| `emotion_level_1` | 允许优先氛围 | 必须排除 |
| --- | --- | --- |
| 开心 | `cheerful`、`warm`、`celebratory` | 明显压抑、孤立画面 |
| 好奇 | `encouraging`、`cheerful`、`calm` | 悲伤或恐惧主导画面 |
| 平静 | `calm`、`warm`、`neutral` | 过度庆祝、强刺激画面 |
| 悲伤、沮丧 | `warm`、`calm`、`encouraging`、`neutral` | `celebratory`、过度欢呼 |
| 愤怒、焦虑、恐惧、压抑 | `calm`、`warm`、`neutral` | `celebratory`、冲突、惊吓或强刺激画面 |

这一步复用已有 `emotion_level_1`，不增加 `visual_tone` 或 `co_emotion` 配图字段。

### 7.2 风险规则

- 成长高光明信片：`risk_judgement.is_hit=true` 时不创建选图任务，不得把风险事件包装为成长庆祝。
- 今日总结：风险日仍可以有总结图片，但候选只允许 `warm/calm/neutral`，必须排除 `celebratory`、冲突、孤立、惊吓等画面。
- 风险判断失败或字段缺失时，后端按保守策略处理：高光不生成；今日总结只使用通用中性图，并记录 `risk_field_missing`。

---

## 8. 配图匹配规则总览

### 8.1 成长高光明信片图片

#### 8.1.1 资格

后端计算，不要求 LLM 输出新资格字段：

```text
highlight_info.has_highlight = true
AND highlight_info.highlight_score >= 7
AND risk_judgement.is_hit = false
```

`dimension_id/dimension` 不参与 MVP 图片匹配；`topic_tags` 也不参与。

#### 8.1.2 后端输入

```json
{
  "asset_type": "growth_postcard",
  "user_id": "user_001",
  "session_id": "session_123",
  "image_scene": "朋友社交",
  "event_period": "day",
  "emotion_level_1": "开心",
  "risk_judgement": {
    "is_hit": false,
    "level": "",
    "category": []
  },
  "recent_image_ids": ["postcard_friends_day_01"],
  "same_day_image_ids": ["summary_friends_day_02"]
}
```

#### 8.1.3 匹配顺序

1. 校验高光资格；不合格则结束，不创建图片使用记录。
2. 查询 `asset_type=growth_postcard AND active=true`。
3. 优先保留 `primary_scene=image_scene` 的图片。
4. `event_period=day/night` 时优先同一 `period`；为 `any` 时不强制昼夜。
5. 根据会话 `emotion_level_1` 排除明显冲突氛围。
6. 排除用户近 30 天使用过的成长明信片图片、同日已使用的明信片图片和当日今日总结图片。
7. 使用稳定轮询或确定性随机选择一张；同一 `session_id` 重试必须复用已落库结果。

兜底顺序：

```text
同场景 + 同时段 + 合适氛围
→ 同场景 + any 时段 + 合适氛围
→ 通用场景 + 同时段 + 合适氛围
→ 通用场景 + any 时段 + 中性氛围
→ 候选只因去重被清空时，选择合规候选中最久未使用图片
→ 无任何安全候选时展示高光默认图，不写普通图片使用记录
```

### 8.2 今日总结图片

#### 8.2.1 资格

- 当天至少有一个有效 `session_insight`。
- 日级 LLM 已成功输出真实 `daily_title/daily_summary`。
- 无有效对话或总结生成失败时，不创建正常选图结果，只展示对应占位状态。

#### 8.2.2 后端输入

```json
{
  "asset_type": "daily_summary",
  "user_id": "user_001",
  "calendar_date": "2026-08-24",
  "image_scene": "朋友社交",
  "event_period": "day",
  "emotion_level_1": "开心",
  "risk_judgement": {
    "is_hit": false,
    "level": "",
    "category": []
  },
  "recent_image_ids": ["summary_friends_day_01"]
}
```

#### 8.2.3 匹配顺序

1. 查询 `asset_type=daily_summary AND active=true`。
2. 优先保留 `primary_scene=image_scene` 的图片。
3. `event_period=day/night` 时优先同一 `period`；为 `any` 时不强制昼夜。
4. 根据日级 `emotion_level_1` 排除明显冲突氛围。
5. 日级风险命中时额外排除所有庆祝、冲突、孤立和惊吓画面，只保留中性/安抚图。
6. 排除用户最近 7 次实际使用的今日总结图片。
7. 使用稳定轮询或确定性随机选择一张；同一 `user_id + calendar_date` 重试复用已落库结果。

兜底顺序与高光一致，但最后使用“今日总结默认占位图”。风险过滤是硬规则，不得为了命中场景或去重而放开冲突氛围。

### 8.3 选图结果

```json
{
  "business_key": "session_123",
  "asset_type": "growth_postcard",
  "image_id": "postcard_friends_day_03",
  "image_scene": "朋友社交",
  "event_period": "day",
  "match_level": "scene_period_mood",
  "fallback_reason": "",
  "rule_version": "image-match-mvp-v1",
  "asset_version": "2026-08-mvp",
  "selected_at": "2026-08-24T12:00:00Z"
}
```

必须保存触发业务键、两项 LLM 匹配字段、最终图片、匹配层级、兜底原因、规则版本、图库版本和选图时间，以便解释和回放。

### 8.4 统一后端匹配算法（可直接实现）

两类图片只替换业务输入和去重窗口，执行同一套算法。算法中的“过滤”是硬条件，“排序”只在仍有多个合规候选时使用。

#### 8.4.1 输入归一

后端先读取 LLM 输出并做契约归一：

| 输入情况 | 归一结果 | 记录原因 |
| --- | --- | --- |
| `image_scene` 缺失或不在 12 类枚举 | `通用` | `invalid_scene` |
| `event_period` 缺失或不在 `day/night/any` | `any` | `invalid_period` |
| `emotion_level_1` 缺失 | 使用 `neutral` 过滤策略 | `emotion_field_missing` |
| `risk_judgement` 缺失 | 高光任务直接拦截；日级只允许通用中性图 | `risk_field_missing` |

归一不能改写原始洞察 JSON；原始值和归一原因都写入选图任务日志。

#### 8.4.2 候选处理步骤

```text
0. 幂等检查
1. 业务资格校验
2. 输入字段归一
3. 按 asset_type 查询 active 图片
4. 执行风险硬过滤
5. 执行情绪氛围硬过滤
6. 按场景/时段建立匹配层级
7. 执行近期使用软去重
8. 同层候选稳定排序并选择
9. 写入选图结果与使用记录
10. 无安全候选时返回业务默认图
```

**步骤 0：幂等检查**

- 高光使用 `session_id`；今日总结使用 `user_id + calendar_date`。
- 已存在成功选图结果时直接返回，不重新抽图、不新增使用记录。
- 已存在处理中任务时复用任务，不并发创建第二个任务。

**步骤 1：业务资格校验**

- 高光必须同时满足 `has_highlight=true`、`highlight_score>=7`、`risk_judgement.is_hit=false`。
- 今日总结必须存在有效日级总结；`daily_mood_score=null` 但有真实总结时仍可选图，完全无有效会话时不可选图。

**步骤 2–5：候选硬过滤**

候选必须同时满足：`active=true`、`asset_type` 正确、资源文件可用、符合风险规则、符合情绪氛围规则。硬过滤后候选为空时，才进入场景/时段降级；任何降级都不能绕过风险过滤。

**步骤 6：匹配层级**

按照下表从高到低逐层查询，找到非空层后停止，不跨层混排：

| `match_level` | 场景条件 | 时段条件 | 氛围条件 |
| --- | --- | --- | --- |
| `scene_period_mood` | `primary_scene=image_scene` | `period=event_period`；输入 `any` 时跳过本层 | 通过情绪/风险硬过滤 |
| `scene_any_mood` | `primary_scene=image_scene` | `period=any` | 通过情绪/风险硬过滤 |
| `generic_period_mood` | `primary_scene=通用` | `period=event_period` | 通过情绪/风险硬过滤 |
| `generic_any_mood` | `primary_scene=通用` | `period=any` | 通过情绪/风险硬过滤 |

当 `event_period=any` 时，直接从 `scene_any_mood → generic_any_mood` 开始；不得为了“补足”而猜测 day 或 night。

**步骤 7：近期使用软去重**

- 先从当前匹配层排除去重窗口内的图片。
- 高光窗口：用户近 30 天的 `growth_postcard` 图片、同日已使用的明信片图片，以及同日今日总结图片。
- 今日总结窗口：用户最近 7 次 `daily_summary` 图片。
- 如果排除后当前层为空，允许恢复被去重排除的合规候选，选择窗口内最久未使用者，并记录 `dedupe_exhausted`。不能直接跳过风险或情绪过滤。

**步骤 8：同层稳定选择**

候选按以下字段升序排序，取第一张：

```text
last_used_at NULL 优先
→ last_used_at 最早优先
→ asset_version 当前启用版本优先
→ hash(business_key + image_id) 稳定打散
```

这样同一业务键重试结果稳定，不依赖数据库无序返回，也不会因服务重启随机换图。

**步骤 9–10：记录与默认图**

- 精确或降级候选均记录 `match_level` 和 `fallback_reason`。
- 候选因去重恢复时记录 `dedupe_exhausted`；因场景/时段降级时追加 `scene_fallback` 或 `period_fallback`。
- 没有任何安全候选时，只返回业务默认图和失败状态；默认图不写入普通图片使用记录，避免污染去重池。

#### 8.4.3 两个功能的算法参数

| 参数 | 成长高光明信片 | 今日总结 |
| --- | --- | --- |
| `asset_type` | `growth_postcard` | `daily_summary` |
| 业务键 | `session_id` | `user_id + calendar_date` |
| 场景/时段来源 | `highlight_info.image_scene/event_period` | `daily_insight.image_scene/event_period` |
| 氛围来源 | 会话 `emotion_level_1` | 日级 `emotion_level_1` |
| 风险命中 | 不生成任务 | 只允许中性/安抚图 |
| 去重窗口 | 近 30 天 + 同日两类图片 | 最近 7 次同类型图片 |
| 无候选结果 | 高光默认图，状态 `blocked_or_unavailable` | 今日总结默认占位图，状态 `unavailable` |

#### 8.4.4 匹配示例

**示例 A：高光精确命中**

```text
输入：image_scene=朋友社交，event_period=day，emotion_level_1=开心，风险未命中
候选：朋友社交/day/温暖图 2 张，其中 1 张近 30 天已用
结果：选择另一张，match_level=scene_period_mood，fallback_reason=""
```

**示例 B：高光候选被去重排除**

```text
输入：image_scene=学习探索，event_period=night
结果：学习探索/night 候选全部在近 30 天使用过，但通用/night 有安全候选
选择：通用/night，match_level=generic_period_mood，fallback_reason="scene_fallback"
若通用/night 也只有已用图片：恢复当前层最久未用的合规候选，追加 "dedupe_exhausted"
```

**示例 C：今日总结风险日**

```text
输入：image_scene=朋友社交，event_period=day，risk_judgement.is_hit=true
结果：过滤 celebratory/冲突/孤立/惊吓图片，只在朋友社交/day 的 warm/calm/neutral 候选中选择
若没有安全场景图：依次降级到通用/day、通用/any；仍不允许使用庆祝图
```

---

## 9. 异常与边界

| 场景 | 系统行为 |
| --- | --- |
| 无有效孩子对话 | 会话分值为 `null`；不生成高光明信片；日级无有效总结时展示无数据状态 |
| `image_scene` 缺失或枚举非法 | 契约校验改为 `通用`，记录 `invalid_scene` |
| `event_period` 缺失或枚举非法 | 契约校验改为 `any`，记录 `invalid_period` |
| 上午说“昨晚踢球” | 使用 `night`，不得按消息时间输出 `day` |
| 代表内容跨多个场景 | 使用 `通用`，不按 Topic Top 1 强行选一个 |
| 代表内容跨昼夜 | 使用 `any` |
| 高光分数达标但命中风险 | 不生成明信片，记录 `risk_blocked` |
| 日级命中风险 | 正常展示真实总结，只使用中性/安抚图 |
| 风险字段缺失 | 高光保守拦截；今日总结使用通用中性图 |
| 精确候选为空 | 按同场景 `any`、通用场景、通用中性图逐级兜底 |
| 候选全部被软去重排除 | 选择安全候选中最久未使用的一张，记录 `dedupe_exhausted` |
| 图片文件加载失败 | 尝试同层下一候选；全部失败时使用业务默认图 |
| 选图服务失败或超时 | 返回已落库旧结果；没有旧结果时使用默认图，不阻断首页其他模块 |
| 同一业务任务重复执行 | 按业务幂等键复用结果，不重复写使用记录 |
| 两类图片选中同一底图 | 高光选图优先排除当日总结图片；不足时允许降级并记录原因 |

---

## 10. MVP 风险与接受方式

### 10.1 共同风险

| 风险 | 原因 | MVP 接受/控制方式 | 观察指标 |
| --- | --- | --- | --- |
| 12 类场景覆盖不足 | 对话事件远多于固定枚举 | 使用 `通用`，记录兜底，不增加开放场景字段 | 通用图兜底率、人工场景错配率 |
| 总结正确但场景归一错误 | LLM 仍可能在粗分类间摇摆 | 固定枚举、例子和契约校验；抽检高频边界 | `image_scene` 准确率 |
| 事件时段误判 | 历史事件与消息时间容易混淆 | 固定证据优先级，证据不足输出 `any` | 昼夜明显错配率、`any` 占比 |
| 固定图库重复 | 单个候选池图片少 | 去重设为软规则，候选耗尽选最久未使用 | 最近窗口重复率、候选耗尽率 |
| 情绪氛围仍较粗 | 一级情绪不能描述完整情绪变化 | 只做“排除明显冲突”，不追求精细美术表达 | 氛围冲突投诉、人工抽检通过率 |
| 风险判断漏判 | 复用原风险字段，非新安全模型 | 风险过滤为硬规则并监控漏判样本 | 风险事件庆祝图数量必须为 0 |
| 两个 LLM 新字段不稳定 | 枚举新增会出现非法值或缺失 | 后端契约校验并降级，不允许直接查不到图 | 非法字段率、通用兜底率 |

### 10.2 成长高光明信片特有风险

| 风险 | MVP 接受/控制方式 | 观察指标 |
| --- | --- | --- |
| 不用高光维度后纪念感变弱 | 先验证事件场景相关性；图片自身保持成长明信片画风 | 高光图片与原文一致率、卡片打开率 |
| 同一场景覆盖不同成长意义 | 不声称图片表达具体维度，维度只在文案中体现 | 用户错配反馈、人工抽检 |
| 30 天去重无法保证 | 每个场景/时段先配 3–4 张，耗尽时软降级 | 30 天重复率、耗尽率 |
| 风险会话失去明信片 | MVP 明确以安全优先；保留原会话风险洞察 | `risk_blocked` 数量和误拦截率 |

### 10.3 今日总结特有风险

| 风险 | MVP 接受/控制方式 | 观察指标 |
| --- | --- | --- |
| 单场景不能代表全天所有会话 | 以 `daily_title/daily_summary` 的主事件为准，无主事件用通用图 | 总结与配图一致率 |
| 不再使用 Topic Top 1 后与话题榜不同 | 产品明确两者含义不同：一个是全天总结，一个是频次统计 | 用户理解问题、人工一致性抽检 |
| 风险日图片过于通用 | 安全优先，只在中性/安抚池内做场景匹配 | 风险日通用图比例、氛围错配率 |
| 最近 7 次不重复无法硬保证 | 120 张首期图库 + 软去重 | 最近 7 次重复率 |

MVP 建议观察门槛：两类图片的明显场景错配率不高于 5%，昼夜明显错配率不高于 3%，非法枚举率低于 1%，高危事件误生成庆祝型明信片为 0。通用图兜底率建议低于 20%，但作为上线观察指标，不作为首版阻断条件。

---

## 11. 功能验收标准

1. 旧版两份提示词文件保持不变；新增版以旧逻辑为主，仅加入本需求字段和规则。
2. 会话级输出保留全部原有字段，`highlight_info` 只新增 `image_scene/event_period`。
3. 日级输出保留全部原有字段，顶层只新增 `image_scene/event_period`。
4. `topic_tags` 不包含配图场景、时段或开放场景描述，且不参与后端选图。
5. 会话级高光配图字段根据 `session_summary + dialogue_snippet` 生成；日级字段根据 `daily_title + daily_summary` 生成。
6. 对上午 11:00 提到“昨晚踢球”的测试，会话高光或日级代表事件 `event_period=night`。
7. 没有可靠时间证据时输出 `any`，没有可靠主场景时输出 `通用`。
8. 高光明信片只在 `has_highlight=true AND highlight_score>=7 AND risk_judgement.is_hit=false` 时生成。
9. 日级风险命中时仍可配图，但所有庆祝、冲突、孤立和惊吓候选被排除。
10. 后端按 `asset_type → image_scene → event_period → emotion_level_1 → risk_judgement → 去重 → 兜底` 执行并记录结果。
11. 候选不足、非法字段、图片加载失败和服务超时均有明确降级，不阻断首页其他模块。
12. 同一 `session_id` 或 `user_id + calendar_date` 重试不生成第二个结果或重复使用记录。
13. 选图结果可追溯 `match_level/fallback_reason/rule_version/asset_version`。
14. 成长高光明信片不再调用原 AI 文生图链路。

### 11.1 验收数据样本

至少覆盖：

- 明确白天事件、明确夜晚事件、历史事件与消息时间冲突、无时间证据。
- 单一场景、多场景并列、具体场景与纯情绪表达。
- 九类一级情绪，重点覆盖负向情绪排除庆祝图。
- 高光 6 分、7 分、风险命中且 8 分。
- 当天无会话、全部分值 `null`、日级风险命中。
- 精确候选为空、去重候选耗尽、图片文件失效、重复任务。

---

## 12. 原有功能修改点

### 12.1 产品规则

| 原规则/当前状态 | 本需求修改后 | 修改原因 |
| --- | --- | --- |
| 今日总结固定图库方向存在，但匹配字段未闭环 | 日级总结直接输出场景/时段，后端统一选图 | 保证总结内容与图片使用同一日级语义 |
| 曾规划 Topic Top 1 决定今日图片 | Topic 榜单与配图彻底解耦 | 今日 Top 1 是全天频次，不能代表完整日级总结 |
| 高光达到分数后调用 AI 文生图 | 达标且无风险后从 `growth_postcard` 固定图库选图 | 降低算力、时延和画风/氛围不稳定 |
| 曾规划按高光维度和 4 个视觉主题建图库 | 高光维度保留展示/统计，图片按总结场景和时段匹配 | 减少字段和图库标签复杂度 |

### 12.2 会话级提示词

- 保留旧版全部输入、输出和业务逻辑。
- 为事件本地时间判断显式列出 `profile.timezone`，但 `calendar_date` 仍由工程侧计算。
- 在 `highlight_info` 只增加 `image_scene/event_period`。
- 明确先生成 `session_summary/highlight_info` 原有字段，再生成两个配图字段。
- `topic_tags` 回归原有话题用途；本新增版不保留 `scene_hint`，也不增加 Topic 配图字段。
- 不新增高光资格、主题、等级、情绪弧线和视觉氛围字段。

### 12.3 日级提示词与 SQL

- 保留旧版日级心情、标题、总结、主情绪、风险和疗愈金句。
- 日级顶层增加 `image_scene/event_period`。
- 移除 `daily_top_topics` 作为新增版日级提示词输入和配图依据。
- SQL 继续独立产出话题/情绪榜单；无需为配图透传场景、时段或 Topic 共现情绪。

### 12.4 后端与数据

- 扩展 `session_insight.highlight_info` 和 `daily_insight` JSON 契约。
- 新增/复用固定图库元数据和两类 `asset_type` 候选查询。
- 新增选图任务、幂等键、使用记录、匹配层级、兜底原因和版本记录。
- 复用 `emotion_level_1/risk_judgement`，不新增重复业务字段。
- 停止高光明信片 AI 文生图调用，但保留旧结果的历史读取兼容。

### 12.5 客户端

- 首页从后端选图结果读取资源，不在客户端复算场景或随机换图。
- 区分无对话、无高光、风险拦截、默认占位、资源加载失败状态。
- 图片失败不得影响标题、总结、高光文案和首页其他模块展示。

---

## 13. 依赖、优先级与成本

| 优先级 | 工作项 | 依赖 | 粗略复杂度 |
| --- | --- | --- | --- |
| P0 | 两份新增提示词与 JSON Schema | 场景/时段枚举冻结 | 低，1–2 人日 |
| P0 | 图片元数据、候选查询、过滤和兜底 | 两类图库素材清单 | 中，3–5 人日 |
| P0 | 幂等选图结果与使用记录 | 数据模型评审 | 中，2–4 人日 |
| P0 | 高光文生图切换为固定图库 | 高光选图服务 | 中，2–3 人日 |
| P0 | 首页真实结果与降级态接入 | 后端接口 | 中，2–4 人日 |
| P1 | 质量埋点与运营看板 | 事件定义、数仓 | 中，2–4 人日 |
| P1 | 标准版按实际分布扩图库 | MVP 线上数据 | 持续运营投入 |

以上为方案评估级估算，不含图片设计与生产时间。正式排期需由后端、客户端、算法/Prompt、设计和 QA 根据现有实现复核。

可独立上线：今日总结和高光明信片可以分别灰度，只要各自提示词字段、`asset_type` 图库和选图接口完整。

必须先统一：12 类场景、三类时段、情绪过滤、风险硬规则、图片元数据和选图结果契约必须使用同一版本，否则两个模块会产生不可解释的差异。

---

## 14. 数据指标与埋点

### 14.1 成功指标

| 指标 | 口径 | MVP 目标 |
| --- | --- | --- |
| 明显场景错配率 | 人工抽检中图片与总结核心事实明显不一致的比例 | ≤5% |
| 昼夜明显错配率 | 明确事件时段与图片时段冲突的比例 | ≤3% |
| 高危庆祝误配数 | 风险高光生成明信片或风险日使用庆祝图的数量 | 0 |
| 通用图兜底率 | `match_level` 落入通用场景的选图次数/全部选图次数 | 观察目标 <20% |
| 字段非法率 | `image_scene/event_period` 缺失或非法次数/LLM 调用次数 | <1% |
| 图片加载成功率 | 图片首次加载成功次数/图片展示请求次数 | ≥99% |

### 14.2 最小埋点

| 事件 | 必要属性 |
| --- | --- |
| `insight_image_task_created` | `asset_type`、业务键、场景、时段、情绪、风险状态 |
| `insight_image_selected` | `image_id`、`match_level`、`fallback_reason`、规则/图库版本 |
| `insight_image_blocked` | `asset_type`、`reason`、风险等级 |
| `insight_image_load_failed` | `asset_type`、`image_id`、错误类型、是否成功降级 |
| `insight_image_impression` | `asset_type`、`image_id`、页面、业务键 |

---

## 15. 待后续数据验证

- 12 类中哪些场景应在标准版拆分，如科技实验、旅行、美食等。
- `day/night/any` 的实际分布和 `any` 是否过高。
- 两类图库哪些底图可安全复用，哪些必须因画幅和纪念感独立制作。
- 近 30 天与最近 7 次去重窗口是否符合实际使用频率。
- 标准版是否需要增加用户“图片不匹配”反馈入口，以形成扩图库依据。
